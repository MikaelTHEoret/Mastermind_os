# Test Environment Configuration
NODE_ENV=test

# Test AI Provider Configuration
VITE_AI_PROVIDER=mock
VITE_AI_MODEL=test-model
VITE_AI_FALLBACK_PROVIDER=mock
VITE_AI_FALLBACK_MODEL=test-model-fallback

# Test Memory System Configuration
MEMORY_SUMMARIZATION_THRESHOLD=5
MEMORY_RELEVANCE_THRESHOLD=0.5
MEMORY_MAX_CONTEXT_MEMORIES=3

# Test Database Configuration
VITE_DATABASE_URL=memory://test
VITE_DATABASE_TYPE=memory

# Test Vector Database Configuration
CHROMA_HOST=localhost
CHROMA_PORT=8001
CHROMA_API_IMPL=rest
CHROMA_API_KEY=test-api-key

# Test Embedding Configuration
EMBEDDING_PROVIDER=mock
EMBEDDING_MODEL=test-embedding-model
EMBEDDING_BATCH_SIZE=128
