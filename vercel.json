{  
  "framework": "nextjs",  
  "buildCommand": "npm install && npm install --save-dev typescript @types/react @types/node && npm run build",  
  "outputDirectory": ".next",  
  "installCommand": "npm install",  
  "rootDirectory": "cosmogram-frontend"  
} 
